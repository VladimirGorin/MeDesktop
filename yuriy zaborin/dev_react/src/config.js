// eslint-disable-line
const KTK = window.location && window.location.hostname
&& (window.location.hostname === 'ktk.comtrading.com' || window.location.hostname === 'ktk.in.ua');

export const MAIN_URI = 'https://comtrading.ua';
export const IS_KTK = KTK;
